<div class="col-12 col-md-6 order-md-1 order-last">
    <h3>
        <%= title %>
    </h3>
</div>

<section class="section">
    <div class="card">
        <div class="card-header">
            User Status Data
        </div>
        <div class="card-body">
            <table class="table table-bordered table-datatable text-center" id="userTable">
                <thead class="table-dark">
                    <tr>
                        <th class="text-center">Name</th>
                        <th class="text-center">Email</th>
                        <th class="text-center">Github Acc</th>
                        <th class="text-center">Stack</th>
                        <th class="text-center">Status Approval</th>
                        <th class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach((u, i) => { %>
                    <tr>
                        <td class="text-center">
                            <%= u.PersonalName %>
                        </td>
                        <td class="text-center">
                            <%= u.Email %>
                        </td>
                        <td class="text-center">
                            <a href="<%= u.ProfileUrl %>"
                                target="_blank" rel="noopener noreferrer">
                                <%= u.ProfileUrl %>
                            </a>
                        </td>
                        <td class="text-center">
                            <%= u.Stack %>
                        </td>
                        <td class="text-center">
                            <% if (u.Approval === 'Y') { %><span
                                class="badge bg-success">Verified</span>
                            <% } else if (u.Approval === 'N') { %>
                            <span class="badge bg-warning">Unverified</span>
                            <% } else { %><span
                                class="badge bg-secondary">Unknown</span>
                            <% } %>
                        </td>
                        <td class="text-center">
                            <form method="post">
                                <button type="submit"
                                    formaction="/approve/<%= u.id %>"
                                    class="btn btn-success"><i
                                        class="bi bi-check-lg"></i></button>
                                <button type="submit"
                                    formaction="/reject/<%= u.id %>"
                                    class="btn btn-danger"><i
                                        class="bi bi-x-lg"></i></button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</section>

<script>
    let usertable = document.querySelector('#userTable');
    let dataTable = new simpleDatatables.DataTable(usertable);
</script>